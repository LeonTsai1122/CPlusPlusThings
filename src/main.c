#include <stdio.h>

/**
 * 程式進入點 (Main function)
 *
 * 這是 C 程式的起點，當程式執行時，會從這裡開始執行。
 *
 * @return 回傳 0 代表程式正常結束。
 */
int main() {
    // 列印 Hello, World! 到標準輸出 (Standard Output)
    // printf 是一個標準函式庫提供的函式，用來輸出格式化的字串
    printf("Hello, World!\n");

    // 程式成功執行完畢，回傳 0 給作業系統
    return 0;
}
